<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="400dp"
    android:layout_height="400dp"
    android:layout_marginTop="150dp"
    android:layout_marginBottom="300dp"
    android:layout_gravity="center_horizontal"
    android:padding="5dp"
    android:orientation="vertical"
    android:id="@+id/search_more_layout"
    android:visibility="invisible">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:gravity="center_horizontal"
        android:text="No more matched result"
        android:textAllCaps="true"
        android:textColor="@android:color/tertiary_text_light"
        android:textSize="24dp"
        android:textStyle="bold" />

    <ImageView
        android:id="@+id/search_more_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_margin="5dp"
        android:layout_weight="1"
        app:srcCompat="@drawable/radar_search" />

    <TextView
        android:id="@+id/search_more_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_horizontal|top"
        android:text="Click Here to Change Search Settings"
        android:textAllCaps="true"
        android:textColor="#009688"
        android:textSize="16dp"

        />
</LinearLayout>
